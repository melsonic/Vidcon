import './App.css'

function App() {
  return (
    <div>
      {/** on clicking it will push the current user into the users queue, from which the server selects two random users to connect with each other */}
      <button onClick={async () => {
        
      }}>Hop Into Room</button>
    </div>
  )
}

export default App
